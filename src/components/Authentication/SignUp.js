import React from "react";
import classes from "./SignUp.module.css";
import Card from "../UI/Card";

const Login = () => {
  return (
    <Card className={classes.mainwrap}>
      <h2 className={classes.registercaption}>Reg test</h2>
    </Card>
  );
};

export default Login;
